import styled from "styled-components";

export const AlignToastContainer = styled.div`
    position: fixed;
    bottom: 20px; /* Distancia desde la parte inferior de la pantalla */
    left: 50%;
    transform: translateX(-50%);
    z-index: 1000;
`